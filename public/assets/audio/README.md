# Tokyo Odd Parade

`tokyo-odd-parade.mp3`는 이 웹사이트를 위해 `scripts/generate-background-music.mjs`로 직접 생성한 밝고 엉뚱한 분위기의 배경음악입니다.

- 외부 음원, 샘플 또는 녹음물을 사용하지 않았습니다.
- 멜로디와 사운드는 코드에서 사인파를 합성해 생성했습니다.
- 재생 시간: 약 29초(반복 재생)

생성 방법:

```bash
npm run generate:music
```
